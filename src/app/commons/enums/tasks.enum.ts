export const enum TaskStatus {
  PENDING = 'PENDING',
  COMPLETED = 'COMPLETED',
}

export const enum TaskPriority {
  HEIGH = 'Alta',
  MEDIUM = 'Media',
  LOW = 'Baja',
}

export const enum OrderBy {
  EXPIRATIONDATA = 'EXPIRATIONDATA',
  PRIORITY = 'PRIORITY',
}

export const enum MantenainceAction {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
}
